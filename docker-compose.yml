version: "3.9"

services:
  imgcodeai:
    image: pregadeshu/imgcodeai:latest
    build: .
    container_name: imgcodeai_app
    ports:
      - "8501:8501"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped